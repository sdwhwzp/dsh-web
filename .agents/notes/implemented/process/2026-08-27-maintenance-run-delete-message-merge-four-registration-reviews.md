# Agent Note: Maintenance run merges dsh-delete-message and reviews four registrations

Status: implemented

## Problem

Fifth same-day /pr-issue-maintenance pass over zhu1090093659/dsh-web under the default scope (open PRs whose assignees include zhu1090093659; Issues out of scope unless explicitly raised). Seven open PRs carry state from four earlier runs; [the preceding record](2026-08-27-maintenance-run-logicprobe-merge-corrupt-branch-block.md) left standing instructions: re-read the #1098/#1144/#1100 threads before acting, and check any rebuilt #1185 head for control-character corruption plus its artifact requirements. This round decided whether rebuilt #1185 is mergeable, which refreshed registrations are acceptance-ready, and opened initial reviews for the never-reviewed ones (#1224, #1235, #1239).

## Decision

- #1185 (dsh-delete-message): approved and merged as 5e85b6ebc. Rebuilt head f25166296 is a single commit on baf7b4016 diffing against origin/dev as exactly community.json (+12) plus market/dist/manifest/plugins.json (+13), pure append, control-character damage gone. node scripts/community-index --check passes locally (41 entries). baf7b4016..1128f2e8a changed neither community.json nor market/dist nor scripts/market-build, so the committed regeneration stayed valid at merge time. Upstream npm remains dsh-delete-message@0.2.1 since 2026-08-25. Both action_required workflow runs (agent-notes-guard, CI) were manually approved to release the first-contribution gate.
- Merge-method deviation, kept on record: gh pr merge --squash instead of the established rebase convention for external index contributions ([prior round](2026-08-27-maintenance-run-logicprobe-merge-corrupt-branch-block.md)). Defensible only because the contribution was already exactly one commit, producing the same history shape a rebase would; for multi-commit rebuilds the rebase convention stands.
- #1100 (termanli, fulltext-search): upstream evidence re-verified via API (commit 3d906468b0 exists, tag v0.1.1 released, scripts/security-test.mjs present covering the forged-sessionId regression, upstream CI green three times including today). Entry now has subcategory dev and bilingual no-auto-dependency caveats for dsh-better-sidebar. Posted consolidated reply; two merge blockers remain: regenerate market/dist onto the post-#1185 dev tail (rank-41 collision otherwise) and drop the contributor-written .agents/notes trio, since guard-agent-notes forbids contributors from that directory.
- #1224 (slywalker2006, dsh-passwords): despite the name, the function is a multi-tenant authenticated gateway for DSH web (bcrypt login, subusers with quotas, ACME automatic HTTPS, rate limiting, audit log). Source audit at upstream HEAD 405c0fe: npm ci clean then npm test 128/128 green on macOS; password storage is bcrypt-hash-only; username and audit fields are AES-256-GCM encrypted with HMAC equal-value indexes; SETUP_KEY, MCP_JWT_SECRET and encryption keys separated with derivation risks documented; certificates written atomically at mode 0600; plugin form valid via dsh.bundle.patch. Compared against official remote-web-ui pairing-based single-owner access: complementary layers, not duplication. Posted initial review with blockers: a real Chinese description and gateway-oriented entry title, rebase plus regeneration (tail conflict certain), and a deploy reproduction covering install, first-admin bootstrap, subuser quota enforcement, and uninstall restoring host config changes their cli patches and restarts.
- #1235 (Zhiyi-Zhao, notion-skill): confirmed plugin-shaped, not a miscategorized skill pack - cordis.patch.yml registers a host-plane skill provider serving skills/notion/SKILL.md with a stdlib-only Python helper; MIT licensed; ci.yml present; provider.test.mjs plus test_notion_api.py; token resolution is NOTION_TOKEN env var or the <dsh_home>/notion/token file. Posted initial review: blockers are rebase-plus-regeneration and a deploy repro (install, skill visible in registry, one live search/read call, uninstall leaving the registry clean); non-blocking suggestion to chmod 600 when creating the token file.
- #1239 (UnusWhite, catppuccin skin): reviewed via the assignees association. Verified v2 manifest conformance, a 381-line Latte/Mocha --dsw-alias-* remap, disclosed small-scope patches.css, bilingual README pair, untouched skin-center package whitelist, order 1002 free of collisions, and preview images that are genuine GUI screenshots matching both palettes. Posted initial review with two procedural blockers: remove the committed .agents/notes trio and fill Local Validation with real commands plus results re-tested on current dev (base 67a747f59 trails by about fourteen commits). Owner review overrode that path within the same hour: the maintainer comment was withdrawn and the PR closed as not qualifying as pure skin collection - it had bundled full-market publish artifacts (market/dist/styles.js, manifest.js, sitemap.xml, manifest/skins.json, tryon-assets) whose rebuild belongs to maintainer-side work at acceptance time; resubmission invited as assets only.
- #1144 (deepsea) and #1098 (agent-plugins-market) stay parked awaiting their authors; thread re-read confirmed no movement, honoring the prior record.

## Alternatives considered

- Landing #1185 with literal rebase to match convention: rejected as post-hoc churn; rewriting pushed dev is forbidden and squash reproduced the single-commit shape the convention aims at. Recorded instead of hidden.
- Proactively unlocking action_required workflow runs on every foreign head: restricted to heads whose reviews are substantively settled (#1185 this round). Deferred for #1100 until its promised push so a dead head does not spend a foreseeable guard failure.
- Hand-resolving the manifest tail conflict to land #1100 or #1235 right after #1185: rejected. Regeneration belongs in contributor branches behind required checks; a mechanical union would hide rank derivation, and all three authors were responding within hours the same day.
- One Agent Note per PR: rejected; the established maintenance-run series consolidates a run's decisions with cross-links to the records it supersedes partially.

## Consequences

- dev advanced to 5e85b6ebc carrying the dsh-delete-message registration (41 entries; rank 41 occupied). All three outstanding index authors were told to regenerate against this tip before their next push.
- #1239 was closed on the owner verdict instead of entering a loop (see its decision bullet); three author loops remain open (#1100, #1235, #1224 content-complete pending listed items). Observed cadence suggests same-day responses; the next runner must re-read those threads before acting.
- Convention refinement recorded: single-commit rebuilds may land via squash; anything larger follows the rebase norm.
- dsh-passwords stability evidence rests on maintainer-executed runs because upstream ships no CI; revisit when published versions move materially past 2.6.3.
- Verification clones and worktrees lived under /tmp and were removed after use; no shared-checkout state was altered besides this note landing on dev.
